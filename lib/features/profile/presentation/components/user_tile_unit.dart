import 'package:flutter/foundation.dart';
import 'package:flutter/material.dart';

class UserTileUnit extends StatelessWidget {
  const UserTileUnit({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}