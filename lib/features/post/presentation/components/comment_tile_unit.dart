import 'package:flutter/material.dart';

class CommentTileUnit extends StatelessWidget {
  const CommentTileUnit({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}