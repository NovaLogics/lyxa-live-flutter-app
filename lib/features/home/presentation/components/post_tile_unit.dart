import 'package:flutter/material.dart';

class PostTileUnit extends StatelessWidget {
  const PostTileUnit({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}